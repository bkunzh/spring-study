spring:
  profiles:
    active: dev
---
spring:
  profiles: prod
  application:
    name: springboot2
  cloud:
    config:
      uri: http://localhost:3301
      label: master
      profile: prod

---
spring:
  profiles: dev
  application:
    name: springboot2
  cloud:
    config:
      uri: http://localhost:3301
      label: master
      profile: dev